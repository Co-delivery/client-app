import 'package:flutter/material.dart';

import 'package:codelivery/app/ui/accept_match/component/body.dart';

class AcceptMatchPage extends StatelessWidget {
  const AcceptMatchPage({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Body()
    );
  }
}